{
  "devDependencies": {
    "turbo": "^1.4.2",
    "@mact/eslint-config": "*"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "turbo-test",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
