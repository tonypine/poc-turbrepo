{
  "devDependencies": {
    "turbo": "^1.4.2"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "turbo-test",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --no-cache",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "migrate": "turbo run migrate"
  },
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
